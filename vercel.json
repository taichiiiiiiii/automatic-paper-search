{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "// 1": "Vercel project config for the on-site theme-submit form.",
  "// 2": "The static `docs/` viewer ships from GitHub Pages — Vercel is",
  "// 3": "deployed only for the `/api/themes` endpoint, so we set",
  "// 4": "buildCommand to a no-op and outputDirectory to an empty marker",
  "// 5": "directory. Vercel still picks up files under `api/` and routes",
  "// 6": "them automatically.",
  "// 7": "",
  "// 8": "After import: set GH_TOKEN in Project Settings → Environment",
  "// 9": "Variables (fine-grained PAT with issues:write only). GH_OWNER /",
  "// 10": "GH_REPO / GH_REF have defaults baked into api/themes.js so they",
  "// 11": "are not required.",
  "buildCommand": "mkdir -p .vercel-empty",
  "outputDirectory": ".vercel-empty",
  "framework": null,
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "access-control-allow-origin", "value": "*" }
      ]
    }
  ]
}
